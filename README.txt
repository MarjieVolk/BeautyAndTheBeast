Repository for Beauty and the Beast video game project.

To run the most recently exported version of the game, double click EclipseProject/Game.jar

Note that Game.jar may not work if it is moved to a different location.